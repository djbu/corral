# Claude Code golden corpus

Each approved real-Claude compatibility capture is stored as
`<normalized-semver>.json`. A missing file is intentional until a human has
reviewed a real contract run; `corral doctor` and the scheduled version watch
surface that gap instead of treating an unreviewed upstream release as known.
