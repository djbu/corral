# M6 real dogfood release gate

**Status:** active — a qualifying real adoption was recorded on 2026-08-09;
the 14-day post window has not completed.

This file is the release evidence for `v0.6.0`. Deterministic tests prove the
pipeline and time-window math, but only real elapsed time can satisfy M6's exit
criterion. Do not tag `v0.6.0` until every required field below contains real
output from one dogfooded repository.

## Procedure

1. Run normal supervised work until one safe exact Bash command has at least
   three correlated manual approvals.
2. Run `corral learnings scan --repo <repo>` and inspect
   `corral learnings show <id> --diff`.
3. Record the repository, learning ID, exact rule, and tree hash/status below,
   then explicitly run `corral learnings adopt <id>`.
4. Use that repository normally for a full 14 days. Do not backdate the daemon
   clock or edit SQLite evidence.
5. At `post_eligible_at`, run `corral learnings report <id> --json`; paste the
   complete result below.
6. Confirm both comparison windows contain at least five sessions and three
   terminal tasks, `blocked_per_session` decreased, and
   `cost_per_terminal_task` did not increase by more than 10%.
7. Confirm the repository tree has no corral-authored write. Only after all
   checks pass may the annotated `v0.6.0` tag be created.

## Real evidence

- Repository: `/Users/danielbecerra/code/research/corral`
- Learning ID: `193985f7-41cc-4378-a4fe-9dfdae0492b6`
- Exact adopted rule: `Bash(rtk go test ./internal/config)`
- Adoption timestamp (UTC): `2026-08-09T20:19:08.232Z`
- Post-window eligible timestamp (UTC): `2026-08-23T20:19:08.232Z`
- Claude Code version: `2.1.226`
- Corral commit: `88ae029bfd974557771ef41cce0ae099fbf06507`
- Repository tree/status before adoption: clean at the commit above;
  `git status --porcelain=v1 --untracked-files=all | shasum -a 256` was
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`.
- Repository tree/status after post window: pending

The dogfood daemon was built from the recorded commit and launched with
`CORRAL_SESSION_PERMISSION_MODE=manual`. This setting applies only to sessions
spawned by that daemon; the account-level Claude Code Auto Mode configuration
was not changed.

One earlier control candidate, `fa9c000d-03fe-4053-ac27-4de8ff773674`, was
mechanically rejected as `unapproved_exact_match`. Its fourth exact command was
approved through a raw attached PTY, so no correlated `session.answered` event
could prove which permission request the operator answered. The evidence was
left intact. The adopted candidate instead has three complete, independently
correlated request/block/answer/resolution chains, 100% evidence coverage, no
failed or denied matches, and no unapproved matches.

### Adoption baseline snapshot

The adoption transaction recorded this measurement:

```json
{
  "phase": "baseline",
  "window_start_ms": 1785097148232,
  "window_end_ms": 1786306748232,
  "metrics": {
    "blocked_events": 7,
    "sessions": 10,
    "blocked_per_session": 0.7,
    "task_cost_usd": 0.755728,
    "terminal_tasks": 3,
    "cost_per_terminal_task": 0.2519093333333333,
    "matched_requests": 3,
    "matched_blocks": 3,
    "skipped_cwds": 0
  },
  "verdict": "baseline"
}
```

All three baseline tasks completed with `status=succeeded`. The baseline is
therefore sufficient under the configured minimums of five sessions and three
terminal tasks.

### Immediate post-adoption smoke

Session `3b5e7ffa-b5f7-4237-83a0-0a5017cd56ac` was spawned after adoption. Its
pinned settings contained only the exact new allow rule:

```json
{"allow":["Bash(rtk go test ./internal/config)"]}
```

The same requested test produced `PreToolUse` event 175, `PostToolUse` event
176, and `Stop` event 178 with zero `PermissionRequest` and zero
`permission.blocked` events. The repository status hash remained the same
clean-tree hash recorded above. This smoke proves future-session injection and
the expected immediate behavior, but it does not replace the real elapsed-time
gate.

A second post-adoption session,
`a4f88f21-bd70-4472-ae23-d18794fb7f57`, repeated the exact command with zero
blocks. Three matching post-adoption DAG tasks also completed with
`status=succeeded`. As of 2026-08-09, applying the reporter's half-open window
queries to the evidence already present gives this **preliminary** sample:

```json
{
  "blocked_events": 0,
  "sessions": 12,
  "blocked_per_session": 0,
  "task_cost_usd": 0.735468,
  "terminal_tasks": 3,
  "cost_per_terminal_task": 0.245156
}
```

That provisional cost per terminal task is 2.680859% below the baseline. The
session count follows the product contract: it includes any repository session
with activity inside the post window, including the orderly checkpoint of the
seven baseline interactive sessions after adoption. These values already meet
the configured sample minimums, but they are not a verdict: later activity in
the still-open window may change them, and only `corral learnings report` at or
after `post_eligible_at` may persist the authoritative post measurement.

### Baseline and post report

Post window pending. At or after `2026-08-23T20:19:08.232Z`, paste the unedited
JSON from:

```sh
corral learnings report <id> --json
```

### Gate decision

- [ ] Real post window spans 14 complete days.
- [ ] Baseline sessions >= 5 and post sessions >= 5.
- [ ] Baseline terminal tasks >= 3 and post terminal tasks >= 3.
- [ ] `blocked_per_session` decreased.
- [ ] `cost_per_terminal_task` increased by no more than 10%.
- [ ] No unapproved repository write occurred.
- [ ] `CORRAL_E2E_REAL_CLAUDE=1 go test ./internal/supervisor -run TestLearningRuleE2E_RealClaude -count=1` passed against the recorded Claude version.
- [ ] `go build ./...`, `go vet ./...`, and `go test -race ./...` passed on the release commit.

Decision: **pending**. `v0.6.0` must remain untagged.
