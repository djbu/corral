# corral M3 design — checkpoint/resume + idle reaping

**Milestone goal (RUNBOOK.md:146-154):** sessions survive daemon restarts and
machine reboots; long-idle sessions are checkpointed and reaped to free
resources, then woken on demand; checkpoints happen at *turn boundaries* so a
resume never lands mid-turn. **Exit criteria:** chaos tests (§5 of RUNBOOK)
green; a machine reboot loses nothing but in-flight turns.

This doc builds on what M1/M2 already shipped. Most of "crash recovery" already
exists — the honest scope of M3 is *idle reaping*, *wake-on-demand*, and
*making `TurnBoundaryVerified` mean something*. We say so plainly and resist
re-building what works.

---

## 0. What already exists (do not rebuild)

Grounding the design in the real tree (verified via source read, not memory):

- **Recovery** — `internal/supervisor/recover.go`. On daemon start, `Recover`
  iterates `sessions` where `desired_state='running'`. For each: if the old
  process is still `Alive(pid, proc_start_ns)` it is **reaped, not re-adopted**
  (the daemon lost the PTY fd across the restart — `reapOrphan` SIGTERM/grace/
  SIGKILL), then relaunched via `--resume <claude_session_id>`. Emits
  `session.orphan_reaped` then `session.resumed` / `session.unresumable`.
  Sessions with `desired_state='stopped'` are **skipped**. This is correct and
  M3 leaves the predicate untouched.
- **Checkpoint** — `internal/checkpoint/resume.go`. `ResumeCheckpointer.Checkpoint`
  today: sets `status=stopping`, SIGTERM to PGID, waits grace, unconditional
  SIGKILL, persists `claude_session_id=s.SessionID`, `status=exited`,
  `exit_signal`, `ended_at_ms`, returns `Token{TurnBoundaryVerified:false}`.
  **It never inspects the transcript** — the `false` is a placeholder from M1.
- **Resume args** — `BuildArgv` (`spawn.go:47`) already emits
  `--resume <ResumeFrom>` when a spec carries `ResumeFrom`, else `--session-id`.
- **`Resumable`** — `resume.go:166`: true iff the row has a `claude_session_id`
  **and** the on-disk transcript exists. Independent of `desired_state`. This is
  the correct wake predicate (see §3).
- **Stale (M2)** — `engine_real.go:200`, `state.stale_after` default 15m. A
  **render-time** flag only. Never kills anything. M3's reaper is a separate,
  action-taking mechanism; we do not overload Stale.

**Absent — M3 adds:** idle reaper (no stub exists), `corral wake`, activity
tracking (`last_activity_ms` column), truthful turn-boundary detection.

---

## 1. Package layout delta

```
internal/
  reaper/            NEW  idle-detection loop; decides which live sessions to checkpoint
    reaper.go
    reaper_test.go
  checkpoint/
    resume.go        EDIT Checkpoint reads transcript tail -> TurnBoundaryVerified
    turnboundary.go  NEW  transcript-tail inspection (last record == completed assistant turn?)
  supervisor/
    recover.go       EDIT extract resumeOne() so wake reuses the resume path
    wake.go          NEW  Wake(name) -> Restore + Spawn, reusing resumeOne()
  store/
    migrations/
      0003_activity.sql  NEW  last_activity_ms
    sessions.go      EDIT bump last_activity on input/attach/hook; TouchActivity()
cmd/corral/
  wake.go            NEW  `corral wake <session>`
internal/config/
  config.go          EDIT State.IdleTimeout (default 0 = off)
  load.go            EDIT parse/default idle_timeout
```

---

## 2. Activity tracking (step 12)

### 2.1 What counts as activity

**Activity = human / external input.** Precisely: `WriteInput` (a human or an
answer typed into the PTY), a client attach, and an inbound hook event. Agent
PTY **output does not count** — a session streaming output for an hour with no
human present is *exactly* what we want to reap. Agent output is *liveness*, not
*activity*; conflating them would make the reaper never fire on a runaway
session. This is written into the migration comment so it is not "fixed" later.

### 2.2 Migration 0003

```sql
-- 0003_activity.sql
-- last_activity_ms: unix-ms of the last HUMAN/EXTERNAL input to this session
-- (WriteInput, attach, inbound hook). NOT bumped by agent PTY output — an
-- idle-from-the-human's-view session is the reap target even while the agent
-- streams. Seeded to started_at_ms for existing rows.
ALTER TABLE sessions ADD COLUMN last_activity_ms INTEGER NOT NULL DEFAULT 0;
UPDATE sessions SET last_activity_ms = COALESCE(started_at_ms, created_at_ms, 0);
```

`store.TouchActivity(ctx, id, ms)` — single-column UPDATE, called from:
- `Registry.WriteInput` (supervisor.go:525) after a successful write,
- attach handler on attach,
- hook ingest path (`hook.received`).

Bumping is best-effort (a failed touch logs and moves on; it must never fail an
input write).

### 2.3 Config

```toml
[session]
idle_timeout = "0s"   # 0 = disabled (default). e.g. "30m" to enable reaping.
```

`config.State.IdleTimeout time.Duration`, default `0`. **Default off is a
deliberate safety choice** — shipping a tool that kills the user's sessions by
default is how a tool gets uninstalled. `[state]`/`[session]` remain
user-file/env only per the repo trust boundary (`repo_allowlist.go`) — a cloned
repo cannot set `idle_timeout`.

---

## 3. Idle reaper (step 13)

### 3.1 The reap set — narrow on purpose

A live session is reaped iff **all** hold:

1. `idle_timeout > 0` (feature enabled), and
2. `agent_state == idle`, and
3. **not attached** (`LiveSession.Attachment == nil`), and
4. `now - last_activity_ms > idle_timeout`.

Condition (2) is load-bearing twice over:

- **Correctness of the checkpoint.** `agent_state==idle` *is* a turn boundary —
  the agent has finished its turn and is waiting. Checkpointing here can never
  lose an in-flight turn, so the reaper never triggers the lossy path. This is
  why M3 can ship truthful checkpoints without stream-json (§5).
- **We deliberately do NOT reap `working` or `blocked`.** `working` = the agent
  is busy; killing it loses the turn. `blocked` = waiting for a permission
  answer; killing it loses the pending prompt (and whether `--resume` restores a
  mid-flight permission prompt is an open spike question). Reaping a
  long-blocked session is a **future** refinement — noted, not built. M2's
  notify path already handles the "nobody's answering" case.

### 3.2 What reaping does — and the `desired_state` decision

Reap = `checkpointer.Checkpoint(ctx, ls, "idle_reap")` **then set
`desired_state='stopped'`**, then emit `session.idle_reaped`.

> **Why `stopped`, not `running`.** This is the design's sharpest edge and got
> it wrong in the first draft. `recover.go:32` relaunches *every*
> `desired_state='running'` row on daemon start. If idle-reap left
> `desired_state='running'`, then: reap 20 sessions overnight → restart the
> daemon → 20 claude processes spring back to life. The daemon would fight the
> reaper. So idle-reap sets `desired_state='stopped'`; recovery skips it; it
> stays checkpointed until explicitly woken.

This forces a clean separation of two properties that a single boolean was
conflating:

| property | means | encoded by |
|---|---|---|
| **resumable** | can be brought back (`claude_session_id` + transcript on disk) | `Resumable()` — already exists, already `desired_state`-independent |
| **should be running now** | daemon should have a live process | `desired_state` |

An idle-reaped session is **resumable** but should **not** be running. `corral
wake` keys off `Resumable()`, never off `desired_state`.

**Do we need to distinguish "idle-reaped" from "user-killed" in the store?**
No. Wake behaves identically for both (both are resumable rows the user asked to
bring back). The distinction that matters for humans — "this stopped because it
was idle" vs "I killed it" — is carried by the `session.idle_reaped` vs
`session.killed` **events**, not by a lifecycle column. So migration 0003 adds
`last_activity_ms` only; no reason column.

### 3.3 The loop

A single daemon-owned goroutine (`reaper.Run(ctx)`), started in daemon startup
after recovery, stopped in shutdown before the store closes. Tick interval =
`min(idle_timeout/4, 1m)`, floor 5s. Each tick: `registry.ListLive()`, filter by
§3.1, checkpoint the survivors sequentially (reuse the shutdown grace; a reap
storm is rare and sequential keeps SIGKILL pressure sane). Ctx cancellation ends
the loop promptly.

New event kind: `session.idle_reaped` (payload: idle duration observed).

---

## 4. Wake (step 14)

### 4.1 `corral wake <session>`

`supervisor.Wake(ctx, idOrName)`:
1. If already live → no-op success (return current row, "already running").
2. Look up the row by **id then name** (`resolveSession`-style; works for a
   non-live row). Error `ErrNotFound` if neither hits.
3. If `!Resumable(row)` → error `session unresumable: <reason>` (no
   `claude_session_id`, or transcript gone).
4. **Name-collision pre-check.** A reaped session ends
   `desired_state='stopped' AND status='exited'` — the *terminal* combination,
   so it has left the `sessions_name_active` partial unique index and another
   session may have taken its name in the meantime. `GetSessionByName(row.Name)`;
   if it returns a **different** id, fail with a clear
   `name %q held by active session %s` — never let the flip below surface as a
   raw UNIQUE-constraint 500.
5. **Persist intent before spawning** (crash-safe, mirrors `Kill`): set
   `desired_state='running'` *first*. Only then `checkpoint.Restore(row)` →
   `Spec{ResumeFrom: claude_session_id, …}` and spawn.
6. Spawn + `resume_count++` + emit `session.resumed` (reason `"wake"`).

Step 6 is the tail `recover.go:recoverOne` already runs for the resume branch.
**Extract only that tail — spawn → `resume_count++` → return the count — into a
private `resumeSpawn`**, and let each caller keep its *own* `Restore`/`Resumable`
gating and emit its own `session.resumed` event. Recovery's stage-specific error
handling (Restore-error marks failed+stopped; Spawn-error is non-fatal and leaves
`desired_state=running`) is pinned by `TestRecover_RestoreErrorMarksFailedAndStopped`
and `recover_test.go:99` — collapsing the stages into one uniform path breaks
those invariants. Recovery keeps its `{resume_count}` payload byte-for-byte; wake
adds `reason:"wake"`.

The engine loop is re-armed for free: `Spawn` fires
`OnLifecycle(EventSessionSpawned)` → `getOrCreateLoop` (idempotent), so a
session whose loop was torn down by the idle-reap's `stopLoop` gets a fresh loop.
Wake adds no `OnLifecycle` call of its own — same as `recoverOne`.

`Resumable()` after a reap: `claude_session_id` is set (`Checkpoint` writes
`s.SessionID`, which *is* claude's own id — fresh spawn passes it via
`--session-id`, so `--resume <that id>` is exactly what claude accepts) and the
transcript `.jsonl` is claude's own file, flushed at the turn boundary the reaper
requires — it survives SIGTERM. So a reaped session is always `Resumable()`.

### 4.2 Auto-wake on answer — DEFERRED to M4 (both HTTP and ntfy)

The original plan was: when `corral answer` targets a resumable-but-not-live
session, wake it first, then deliver the answer — HTTP-path-only, ntfy left
alone. **This is deferred to M4 in full, and the reason is not the ntfy gate.**

A freshly `--resume`d claude has **no observable ready-for-input signal**.
`Spawn` returns as soon as `pty.StartWithSize` succeeds and the `LiveSession` is
registered — nothing in the tree observes claude's TUI reaching a prompt.
`agent_state` only moves off `starting` when a hook fires, and no hook fires on
"TUI ready." Delivering an answer immediately after wake would write
bracketed-paste framing into a PTY whose reader is mid-terminal-init (raw-mode
entry, input-buffer clears): the input can be silently swallowed and the answer
is gone, with no way to detect it. That is strictly worse than the current
`ErrNotLive` → 409, which tells the caller plainly to `corral wake` first.

This is the **same** unknown on both transports — HTTP-only does not dodge it,
it just moves it somewhere with less scrutiny. So M3 ships **explicit `corral
wake` only**; `handleAnswer` keeps its `ErrNotLive` → 409 path exactly as
shipped, and the ntfy reply path stays byte-for-byte as shipped.

**M4 spike question (the actual deliverable this defers):** *how do we know a
resumed claude is ready to accept typed input?* Answer that first; auto-wake
over both HTTP and ntfy (the latter also needing the gate-3 amendment) follows
from it.

---

## 5. Truthful turn-boundary checkpoint (step 15)

### 5.1 Make `TurnBoundaryVerified` mean something

Today `Checkpoint` returns `TurnBoundaryVerified:false` unconditionally, and
**every caller discards the returned `Token`** (`supervisor.go:559`, `:589`,
`shutdown.go:112`, and the `daemon.go:433` adapter all use `_, err :=` / err-only).
So "make it mean something" is two jobs: compute the real value, and give it an
observable *sink*.

**Parser.** `turnboundary.go` adds `LastRecordIsCompletedTurn(transcriptPath)
(bool, error)`. The session `.jsonl` is append-only JSONL, one JSON object per
line. Only `type ∈ {user, assistant}` with `isSidechain != true` are main-thread
conversation records; everything else (`system`, `attachment`, `mode`,
`last-prompt`, `ai-title`, `pr-link`, `file-history-*`, `queue-operation`, …) is
bookkeeping and **the file's literal last line is frequently one of these** — a
naive "tail one line, check `stop_reason`" is wrong. Scan **backward** for the
last conversation record; return `true` iff it is `assistant` with
`message.stop_reason == "end_turn"`. **`end_turn` only** — `stop_sequence` and
especially `max_tokens` (a *truncated* turn) are not clean boundaries.

**Bounded read.** Transcripts run multi-MB (sampled 6.8 MB and 11.3 MB), and
`Checkpoint` runs across every live session on daemon shutdown. Do not read whole
files line-by-line: `Seek` a fixed tail window from `SEEK_END` (256 KB), discard
the first partial line, scan the remainder backward. If no conversation record
lies in the window, return `false` (don't escalate to a full-file read) — a
transcript whose last 256 KB is entirely non-conversation is not a case for a
confident `true`. Missing file, empty file, unparseable trailing line, and
no-conversation-record all → `false`.

**Sink: the checkpoint *event payload*, not a column.** Nothing reads
`TurnBoundaryVerified`, and turn-boundary-verified is a property of *a checkpoint
occurrence*, not of the session row — a session checkpointed three times has three
answers, and a column keeps only the last. No migration. Instead the callers stop
discarding the `Token` and stamp `turn_boundary_verified` into the lifecycle event
they already emit: `session.killed` (Kill) and `session.idle_reaped` (CheckpointIdle).
That payload is the observable that makes M3's "checkpoints happen at turn
boundaries" claim testable. The daemon-shutdown path (`checkpointLive`) emits **no
per-session lifecycle event**, so there the value goes to a structured Info log
(`turn_boundary_verified` field) rather than a new event kind — we don't invent an
event just to carry one bool on the shutdown path. (This amends an earlier draft that said
"the row" — that was wrong.)

**Read before SIGTERM — load-bearing, not a preference.** Compute the value in the
same block that flips `status=stopping`, *before* the first `killGroupTolerant`.
After SIGTERM claude may flush the in-flight turn during its grace window, so a
post-signal read could report `true` for a checkpoint that was *not* taken at a
boundary — inverting the field's meaning from "was this session at a turn boundary
when we decided to stop it" to "did the transcript happen to end cleanly after we
killed it."

For the **reaper** path the value is *expected* `true` (§3.1 cond 2 — the agent is
idle at a turn boundary), but it **may be `false` under transcript flush lag**: the
on-disk `.jsonl` is not flushed incrementally, so a Stop hook can flip
`agent_state=idle` before the `end_turn` record reaches disk (spike/resume/NOTES.md
§3). Recording `false` there is *honest* — it means "could not confirm a boundary"
— not a bug, and we deliberately do **not** engineer a flush-wait poll around it
(new latency on every kill/shutdown for a field with no runtime readers). For the
**shutdown** path of a `working` session `false` is the common, correct answer.

### 5.2 What we deliberately do NOT do in M3: re-send

The spike finding ("session `.jsonl` isn't flushed incrementally; a SIGKILL
mid-turn loses the in-flight turn; recovery should re-send the last prompt")
motivated a re-send mechanism. **M3 does not build it, on purpose:**

- For an **interactive PTY session there is no captured record of the last
  prompt.** The user typed it into a TUI. We have `Screen` (grid state, not
  input history) and `output.log` (capped, `O_TRUNC` on each spawn,
  diagnostics-only per `outputlog.go`). Nothing durable holds user input. There
  is nothing to re-send.
- The finding is really about `-p` / **stream-json headless** sessions, where a
  structured prompt record exists — that is **M4**.
- The reaper never loses an in-flight turn (§3.1). The only lossy path is
  shutdown-checkpoint of a `working` session, and RUNBOOK's own exit criterion
  is "a reboot loses nothing **but in-flight turns**." Recording
  `TurnBoundaryVerified=false` + emitting an event + accepting the loss is
  therefore *within* the milestone, not a gap.

So M3 step 15 = **detect and record turn-boundary state honestly.** Re-send is
deferred to M4 and this deferral is written down here so it is not silently lost.

---

## 6. Chaos tests (step 16)

Mapped to RUNBOOK §5:

1. **Idle reap round-trip** — spawn (fake CLI), drive to `idle`, advance a fake
   clock past `idle_timeout`, tick reaper → assert `status=exited`,
   `desired_state=stopped`, `session.idle_reaped` emitted, `Resumable()` true.
   Assert the `turn_boundary_verified` key is **present** in the
   `session.idle_reaped` payload — never assert its value here (flush lag makes
   `true`/`false` nondeterministic under a real CLI; value is pinned only in
   `turnboundary_test.go` over fixture transcripts).
2. **Wake round-trip** — from the reaped state, `Wake` → assert live,
   `desired_state=running`, `--resume` in argv, `session.resumed(reason=wake)`.
3. **Attached session immune** — attached + idle + past timeout → reaper does
   **not** touch it.
4. **Working/blocked immune** — same, for `agent_state ∈ {working, blocked}`.
5. **Daemon restart after reap** — reaped (`desired_state=stopped`) row is
   **not** resurrected by recovery (guards against the first-draft bug).
6. **Restart of a running session** — unchanged M1 behavior still green
   (orphan_reaped → resumed).
7. **Answer to a reaped session** — `handleAnswer` returns `409 session_not_live`
   (auto-wake deferred, §4.2); a subsequent `corral wake` then `answer` succeeds.
8. **Wake name-collision** — reap session `foo`, create a new live `foo`, then
   `Wake(old-id)` → clean `name held by active session` error, not a 500.

Fake clock + fake CLI (already used in supervisor/checkpoint tests) keep these
deterministic and `-race`-clean.

---

## 7. Ordered step list

- **12** Activity tracking: migration 0003, `TouchActivity`, bump sites, config
  `idle_timeout` (default 0).
- **13** Reaper package + daemon wiring; `session.idle_reaped` event; reap sets
  `desired_state=stopped`.
- **14** `resumeSpawn` tail extraction; `supervisor.Wake` (name-collision
  pre-check, desired_state-first); `corral wake` CLI + `POST
  /v1/sessions/{idOrName}/wake`. Auto-wake-on-answer deferred (§4.2).
- **15** `turnboundary.go`; `Checkpoint` stamps real `TurnBoundaryVerified`.
- **16** Chaos tests (§6); close M3; tag `v0.3.0`.

## 8. Deferred to M4 (recorded so they aren't lost)

- Re-send of the last prompt for stream-json headless sessions (§5.2).
- **Auto-wake on answer (both HTTP and ntfy).** Gated on the M4 spike: *how do
  we know a resumed claude is ready to accept typed input?* There is no
  ready-for-input signal today, so delivering an answer to a just-woken session
  can silently lose it. ntfy additionally needs a gate-3 amendment. (§4.2)
- Reaping long-`blocked` sessions (needs certainty that `--resume` restores a
  pending permission prompt) (§3.1).
- `blocked_count` surfacing (carried from M2).
